/* Aqui vai constar a configuração do BABEL */

// Exportamos um objeto dentro do Bable
// Essa é uma configuração padrão
module.exports = {
    // Conjunto de configurações criadas pelos terceiros
    presets: [
        '@babel/preset-env',
        '@babel/preset-react'
    ],
};